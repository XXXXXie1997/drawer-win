# drawer

**随放随取**

drawer 是一个二维内容抽屉。可以将文本、文件、文件夹和截图等放入，自由整理位置并在需要时随时取出。

收集网页片段、暂存截图，放入文本并编辑、或在多个窗口之间传递文件，都可以在同一块画布上完成。

## 开始使用

适用于 **Windows 11 x64**，双击 `启动 drawer.cmd` 即可运行。

1. 将鼠标移到设置的停靠位置，点击弹出的黑色区域。
2. 拖入内容，或使用 `Ctrl+V` 粘贴；拖动对象整理位置。
3. 需要时复制、拖出，或双击文字选取其中一部分。

右键拖动画布可以平移，`Ctrl+滚轮` 可以缩放。更多操作在画布右键菜单中，停靠位置可通过通知区域图标里的“偏好设置”调整。

macOS 版：[drawer-mac](https://github.com/XXXXXie1997/drawer-mac)。

## 主要功能

- **随时呼出**：收起后在屏幕边缘保留细边提示，鼠标移上去显示完整把手，点击或拖入内容即可展开，离开后平滑收回。支持顶部、左侧和右侧停靠。
- **拖动调整停靠位置**：在偏好设置中进入位置编辑模式，拖动把手沿屏幕边缘调整，确认保存；取消或按 `Esc` 保留原位置。
- **多个独立抽屉**：最多创建 5 个抽屉，内容和撤销记录各自独立。把手标识支持文字和 emoji，可统一调整位置，也能在抽屉之间复制或拖放内容。
- **独立呼出快捷键**：在抽屉列表中直接查看、设置全局快捷键；支持修改、清除和冲突提示，重命名不会改变绑定。列表同时显示停靠位置、尺寸、主题和对象数量。
- **按习惯配置**：用滑动条分别设置抽屉宽高，占屏幕的 10%–90%；从 8 种主题配色中选择并预览；在配置窗口统一保存调整。
- **自由整理**：文字、文件和图片混合摆放，支持多选、框选和整体移动；对象自动吸附网格、避让重叠。
- **落点跟随选择**：选中或移动对象时更新蓝色落点，方便继续放入内容。
- **文字直接编辑**：双击修改文字，也可以只选取其中一段复制或拖出。
- **跨应用取用**：将内容复制或拖到支持的应用中，抽屉里的对象继续保留。
- **可缩放画布**：平移、缩放查看内容，也能通过“显示全部”找回画布上的所有对象。
- **自动保存**：重启后恢复内容和布局，支持会话内撤销与重做。内容保存在本机。

文件以引用方式放入抽屉，从抽屉中移除不会删除源文件。

## 更新日志
### v1.3
1. 可调整把手和抽屉的位置
2. 支持配置呼出抽屉快捷键
3. 支持新增抽屉（上限为5），并支持单个抽屉修改标签
4. 可单独配置抽屉的尺寸和主题
5. 选中托盘中对象时更新落点（十字光标）
6. 弹出动画更圆滑,并修复两侧展开时托盘与屏幕边缘短暂断开的问题
7. 托盘收起时也会保留一点可见区域防止用户忘记托盘位置
8. 增加可选主题
9. 支持设置开机自启


