## 2026-09-08 已完成
1. [√]通过设置调整把手和托盘的位置
2. [√]能用快捷键呼出托盘
3. [√]设置中可以修改除了主要操作之外所有的快捷键
4. [√]设置中可以添加托盘，添加后会新增一个托盘停靠，并在把手处添加托盘编号，支持修改编号，禁止重复但内容不强制数字，可以用其他文字和emoji，托盘上限暂定为5
5. [√]设置中可以调整托盘的显示大小
6. [√]主题可配置

## 2026-09-09 已完成
1. [√]选中托盘中对象时，要更新落点（蓝色十字光标）
2. [√]弹出动画要更圆滑,并排查修复两侧展开时托盘与屏幕边缘短暂断开的问题
3. [√]托盘收起手，把手要稍微留一个边边出来，防止用户忘记托盘位置
4. [√]多添加几个主题配色
5. [√]可以设置开机自启

## 2026-09-10 收集中
1. 自动排列/自动堆叠
    - 将抽屉内对象自动按类型在视口内自动排列
    - 将抽屉内同类型文件堆叠在一起，可查看堆叠对象详情和统一移动、取用（涉及应用内文件夹、可能会影响当前存取方式和数据结构）
2. 放入的图片文件直接展示，不采用其他文件的图标+文件名格式
3. 支持配置贴入图片默认大小，支持调整抽屉中图片的大小
4. 支持双击空白区域直接添加新的文本（一定程度上取代记事本）
5. 文本框的大小也可以调整
6. 去掉抽屉边界，改为虚拟面板按需渲染，添加路径点（选择路径点切换视口到路径点位置）跳转
7. 抽屉要支持中转（剪贴板）模式和收纳模式
    - 中转模式：贴入文件保持复制文件、文件夹的引用，拖出则是粘贴源文件
    - 收纳模式：模拟一个真正的文件夹，放入的文件、文件夹、图片时，直接移动文件到特定文件夹